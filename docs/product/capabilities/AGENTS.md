# Capability Contracts

## Purpose

Owns current, falsifiable user-visible behaviour contracts.

## Local Contracts

- Name records `CAP-<number>-<slug>.md`.
- State present or approved target behaviour, boundaries, and executable evidence.
- Do not record temporary phase progress here; use `docs/changes/active/`.

## Child DOX Index

- `CAP-001-static-site-and-route-compatibility.md` — static site, preserved routes, homepage listing window.
